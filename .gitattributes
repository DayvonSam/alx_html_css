# normalize text files and keep line endings consistent
* text=auto

# ensure web files use LF in the repo (useful for validators and CI)
*.html text eol=lf
*.css  text eol=lf
*.js   text eol=lf
README.md text eol=lf
